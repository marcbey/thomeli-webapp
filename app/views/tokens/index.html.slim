h1 Hallo lieber Kunde :-)
h2 Wie wollen Sie ihr Bild haben?

a> href='#{photo_url( @asset )}' herunterladen
a> href='#{photo_url_inline( @asset )}' im Browser anzeigen
form method="POST"
  label An diese E-Mail-Addresse schicken:
  input type="text" name="email"
  input type="hidden" token="#{params[:token]}"
  input type="hidden" csrf-token="#{form_authenticity_token}"
  button Senden

